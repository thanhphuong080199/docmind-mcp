# docmind retrieval eval — 2026-07

**Setup:** docs ingested: `kafka-operations-guide.md`, `redis-caching-patterns.md`,
`docker-networking-basics.md` (sample docs in `docs-inbox/`, 5 chunks each);
similarity-threshold: 0.0; topK 3 per query; embedding model `nomic-embed-text`.

**Method:** retrieval metrics recorded via direct MCP `tools/call search_docs`
(scores are cosine similarity). Claude tool-selection was spot-checked with a
headless Claude Code session (`claude -p`, docmind registered over HTTP) — see
row 6. Scores are model-specific to `nomic-embed-text`.

| # | Question | Expected source doc | Top score | Right doc in top 3? | Claude used it? | Notes |
|---|----------|--------------------|-----------|--------------------|-----------------|-------|
| 1 | What replication factor should production Kafka topics use? | kafka-operations-guide | 0.696 | yes (3/3) | n/t | answer chunk in top 3 |
| 2 | How do I implement cache-aside with Redis? | redis-caching-patterns | 0.717 | yes (2/3) | n/t | 3rd hit was Kafka cache-invalidation chunk (related) |
| 3 | Difference between bridge and host networking in Docker? | docker-networking-basics | 0.839 | yes (3/3) | n/t | best score of the eval |
| 4 | How do I choose the number of partitions for a Kafka topic? | kafka-operations-guide | 0.650 | yes (3/3) | n/t | lowest correct top score |
| 5 | Can I use Kafka events to invalidate Redis cache entries? | kafka + redis (cross-doc) | 0.782 | yes (both docs) | n/t | kafka #1, redis #2–3 — cross-doc retrieval works |
| 6 | Which Redis eviction policy implements an LRU cache? | redis-caching-patterns | 0.706 | yes (3/3) | **yes** | headless Claude called search_docs, answered `allkeys-lru`, cited doc + id |
| 7 | consumer group rebalancing (keyword-only) | kafka-operations-guide | 0.721 | yes (3/3) | n/t | keyword query fine; tail chunks 0.49/0.48 |
| 8 | How do containers on the same Docker network resolve each other by name? | docker-networking-basics | 0.827 | yes (3/3) | n/t | |
| 9 | How do I configure Kubernetes ingress TLS certificates? | none (unanswerable) | 0.545 | n/a | n/t | junk returned at 0.52–0.55 — motivates threshold |
| 10 | How do I publish a container port to the host? | docker-networking-basics | 0.788 | yes (3/3) | n/t | |

n/t = not tested per-row; Claude behavior spot-checked on row 6.

## Verdict
- Retrieval hit rate (right doc in top 3): 9/9 answerable questions
- Observed top-score range for correct hits: ~0.650–0.839  → threshold must stay below 0.65
- Observed score range for the unanswerable question: ~0.517–0.545 → threshold must stay above 0.55
- **Chosen threshold: 0.60** (midpoint of the 0.545–0.650 gap)
- Failure notes: none for retrieval. Same-doc *supporting* chunks often score
  0.47–0.60 and will be filtered at 0.60; the primary answer chunk always scored
  ≥ 0.65, so this only trims context, not answers.
- Tool descriptions left unchanged: the spot-check showed Claude selecting
  search_docs unprompted with a sensible query; no misbehavior observed to fix.
